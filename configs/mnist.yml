# DDPM MNIST 28x28 — full config

dataset: mnist

image_size: 28
base_channels: 64
timesteps: 1000

epochs: 200
batch_size: 256
lr: 0.0002
warmup_steps: 500

save_interval: 10
sample_interval: 10
num_workers: 0
resume: false
